import React from "react";
import Grid from "./Grid";

const App = () => {
  return (
    <div>
      <Grid />;
    </div>
  );
  //  return Array.from({ length: 12 }, (item,i) => <Cell key={i}/>)
};

export default App;
